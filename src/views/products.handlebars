<!DOCTYPE html>
<html>
<head>
  <title>Products</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 20px;
    }

    h1 {
      margin-bottom: 20px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    th, td {
      padding: 10px;
      text-align: left;
      border-bottom: 1px solid #ddd;
    }

    .add-to-cart-button {
      background-color: #4CAF50;
      color: white;
      border: none;
      padding: 8px 12px;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      font-size: 14px;
      margin-top: 10px;
      cursor: pointer;
    }

    .add-to-cart-button:hover {
      background-color: #45a049;
    }
  </style>
</head>
<body>
  <h1>Products</h1>
  <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Description</th>
        <th>Price</th>
        <th>Stock</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {{#each products}}
      <tr>
        <td>{{title}}</td>
        <td>{{description}}</td>
        <td>{{price}}</td>
        <td>{{stock}}</td>
        <td>
          <button class="add-to-cart-button" onclick="addToCart('{{_id}}')">Add to Cart</button>
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>

  <script>
    function addToCart(productId) {
      // hacer cositas acá
    }
  </script>
</body>
</html>
