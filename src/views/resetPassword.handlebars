<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Recovery</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-6 mt-4">

            <form class="p-4 recovery-form" action="http://localhost:3030/resetPassword" method="post">
            <h2 class="text-center mb-4">Cambiar contraseña</h2>
            <div class="text-center mt-4">
                    <p>Ingresá el mail de la cuenta que querés cambiar la contraseña</p>
            </div>
                <div class="form-group">
                    <input type="email" class="form-control" id="email" name="email" placeholder="ingresá tu mail" required>
                </div>

                {{#if recoveryInfo }}
                    <div class="alert alert-danger mt-4" role="alert">{{recoveryInfo}}</div>
                {{/if}}

                <button type="submit" class="btn btn-success btn-block">Enviar link de cambio</button>
            </form>
        </div>
    </div>
</div>

</body>
</html>
