<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">

    <style>
    .login-form {
        background: #e1e1e1;
        border-radius: 1em;
    }

    @media (max-width: 576px) {
        .login-form {
            margin: 1.5rem;
        }
    }
</style>

</head>
<body>


<div class="row justify-content-md-center">
    <div class="col-md-4 col-xs-12 mt-4">

        <form class="p-4 login-form" action="http://localhost:3030/login" method="post">
            <label for="">Ingresá tu mail y contraseña</label>
            <div class="form-group mb-4">
                <input type="email" class="form-control" id="usr" name="usr" aria-describedby="emailHelp"
                    placeholder="Ingresa tu mail" required="required">
            </div>

            <div class="form-group mb-4">
                <input type="password" class="form-control" id="pass" name="pass" required="required" placeholder="Contraseña">
            </div>

            {{!-- <div class="form-group form-check mb-4">
                <input type="checkbox" class="form-check-input" id="exampleCheck1">
                <label class="form-check-label" for="exampleCheck1">Registrar nuevo usuario</label>
            </div> --}}

            <button type="submit" class="btn btn-success"><i class="fas fa-sign-in-alt"></i> Iniciar sesión</button>
            
            {{#if sessionInfo.errorMessage }}
            <div class="alert alert-danger mt-4" role="alert">{{sessionInfo.errorMessage}}</div>
            {{/if}}
        </form>
    </div>
</div>

<script>
    document.getElementById('usr').focus();
</script>


</body>
</html>